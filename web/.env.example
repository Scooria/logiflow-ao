# Se não definido, a aplicação corre em modo demonstração com dados de exemplo
# (ver src/lib/mockData.ts) — útil para pré-visualizar a UI antes do backend
# do Passo 2 estar disponível.
VITE_API_BASE_URL="http://localhost:3000"
VITE_DEMO_TENANT_ID="demo-tenant"
